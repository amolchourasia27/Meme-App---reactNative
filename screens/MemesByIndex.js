import React from 'react';
import {View, Text} from 'react-native';

const MemesByIndex = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignContent: 'center'}}>
      <Text style={{color: 'black'}}>Hello from Memes by index</Text>
    </View>
  );
};

export default MemesByIndex;
